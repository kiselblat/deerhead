import React from 'react';
import './style.scss';

export const Footer = (props) => {
    return(
        <p className={`Footer`}>Problems? Contact <a href="mailto:thomas@blackforestinnmpls.com">the webmaster</a></p>
    )
}